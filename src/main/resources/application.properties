# Spring JPA og Hibernate-indstillinger
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true

# Datasource-indstillinger for Azure SQL Database
#spring.datasource.url=jdbc:sqlserver://${AZURE_SQL_SERVER_NAME}.database.windows.net:1433;database=${AZURE_DATABASE_NAME}
#spring.datasource.username=${AZURE_USERNAME}
#spring.datasource.password=${AZURE_PASSWORD}

# Aktiverer Azure-identitet (optional, hvis du bruger Managed Identity)
#spring.cloud.azure.enabled=true

# Loggerindstilling for mere information vedrørende webanmodninger

#logging.level.org.springframework.web=DEBUG
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

# Data are setup using the data.sql file. This value ensures that spring boot has created the table before the insert statements are executed
spring.jpa.defer-datasource-initialization=true
# Ensure that the data.sql file is executed every time the application starts
spring.sql.init.mode=always

spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_USERNAME}
spring.datasource.password=${JDBC_PASSWORD}
#JDBC_DATABASE_URL=jdbc:mysql://localhost:3306/kohberg

logging.level.org.springframework.web=DEBUG